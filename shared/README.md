# Shared

This folder contains shared configuration, constants, and utility functions used by multiple services.

- `config/` — service configuration
- `constants/` — cross-service constants
- `utils/` — small utility helpers
